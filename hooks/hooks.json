{
  "SessionStart": [
    {
      "matcher": "",
      "hooks": [
        {
          "type": "command",
          "command": "cat >/dev/null || true; code-review-navigator status",
          "timeout": 10
        }
      ]
    }
  ],
  "PostToolUse": [
    {
      "matcher": "EnterWorktree",
      "hooks": [
        {
          "type": "command",
          "command": "cat >/dev/null || true; code-review-navigator build >/dev/null 2>&1 &"
        }
      ]
    },
    {
      "matcher": "Write|Edit|Bash",
      "hooks": [
        {
          "type": "command",
          "command": "cat >/dev/null || true; code-review-navigator update --skip-flows",
          "timeout": 30
        }
      ]
    }
  ]
}
